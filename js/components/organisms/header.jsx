export default function({ React }) {
  /*
  <Header />
  */
  const Header = React.createClass({

    render : function() {
      return (
        <header>
          Header!
        </header>
      )
    }
  });

  return Header;
};
